# Handoff: UI-044 Q4 Single-Column + Dog-Lottie Implementation

## 1. Metadata
1. Task ID: UI-044
2. Title: Implement Q4 single-column dictation structure and `image-dog.lottie` mapping
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Updated question visual mapping for Q4 from static dog SVG to `image-dog.lottie`.
2. Updated dictation rendering condition so Q4 follows single-column structure used for parity screens.
3. Preserved Q8 no-image single-column behavior and existing default dictation two-column behavior for other questions.
2. What was intentionally not done:
1. no audio gating logic changes.
2. no submit CTA behavior changes.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/features/student/dictation-images.ts`
2. `web/src/features/student/student-mode.tsx`
2. New files:
1. `handoffs/UI-044.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. static gates green.
3. Manual checks:
1. Q4 now renders single-column flow with image placed between audio button and answer input.
2. Q4 image source resolves to dog lottie asset.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-036`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. none critical.
3. Follow-up required:
1. QA signoff and push if requested.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
