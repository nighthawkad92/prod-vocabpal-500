# Handoff: UI-036 Question-Shell Spacing + Collect Counter Implementation

## 1. Metadata
1. Task ID: UI-036
2. Title: Implement question-shell spacing, left-aligned counter row, and collect-star indicator
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Set active question-screen top spacing to deliver 48px from utility bar to question row.
2. Set 8px gap between `Question X of 10` row and progress bar.
3. Set 48px gap between progress bar and question card.
4. Left-aligned `Question X of 10` element.
5. Added `Collect X` inline counter with star icon and vertical separator.
2. What was intentionally not done:
1. no changes to scoring logic, API contracts, or teacher flow.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/features/student/student-mode.tsx`
2. New files:
1. `handoffs/UI-036.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. all static gates pass.
3. Manual checks:
1. question + collect row left aligned with separator.
2. required spacing updates applied in active question state.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-028`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. none critical.
3. Follow-up required:
1. QA confirmation and PM signoff.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
