# Handoff: UI-035 Submit CTA + Question/Answer Formatting Updates

## 1. Metadata
1. Task ID: UI-035
2. Title: Implement submit CTA icon-state polish and question/answer text formatting updates
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Submit CTA now hides star during loading/waiting states and shows icon only in ready state.
2. Submit star is now rendered as a filled icon using ink-color mask and larger visual size.
3. Submit CTA vertical padding increased to 12px (`py-3`) with `h-auto`.
4. Removed prompt container styling on question prompts.
5. Added `Q{displayOrder}:` prefix to both MCQ and dictation prompts.
6. Answer options now render sentence case (first letter uppercase, remainder lowercase) while retaining original option value for submission/scoring.
2. What was intentionally not done:
1. no changes to scoring, API payloads, or teacher screens.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/features/student/student-mode.tsx`
2. `web/src/assets/icons/star.svg`
3. New files:
1. `handoffs/UI-035.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. all static gates pass.
3. Manual checks:
1. submit icon only appears when submit CTA is in ready state.
2. question lines show `Qn:` prefix and no prompt container box.
3. MCQ option labels render sentence case.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-027`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. sentence-case transform is display-only and may alter visual capitalization of acronyms.
3. Follow-up required:
1. QA review and PM signoff.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
