# Handoff: QA-017 Entry Viewport + Onboarding Copy Validation

## 1. Metadata
1. Task ID: QA-017
2. Title: Validate entry viewport alignment and onboarding copy/state regression gates
3. Owner Agent: QA
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Ran static quality gates after entry layout/copy update.
2. Verified student subtitle copy switches between step 1 and step 2.
3. Verified onboarding progress bar removal from entry state.
4. Verified entry title change to `Baseline Test` in student and teacher login states.
5. Verified active student question progress bar remains present.
2. What was intentionally not done:
1. no remote deployment QA in this task.
2. no performance/load testing.

## 3. Files and Artifacts
1. Files changed:
1. `agent_board.md`
2. New files:
1. `handoffs/QA-017.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`
4. source checks via `rg` to confirm copy removal/addition.

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. no TypeScript, lint, or build regressions.
3. Manual checks:
1. entry lockup/form centering behavior applied for student and teacher login states.
2. form width cap (`450px`) and lockup gap (`24px`) applied.
3. subtitles match exact requested copy.

## 5. Downstream Impact
1. Tasks unblocked:
1. `PM-DS-018`
2. Interfaces changed:
1. none (verification task).
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. final visual check on target tablet browsers is still recommended before classroom pilot sessions.
3. Follow-up required:
1. PM final signoff and optional push/deploy step.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
