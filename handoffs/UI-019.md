# Handoff: UI-019 Muted-to-Ink Token Harmonization

## 1. Metadata
1. Task ID: UI-019
2. Title: Implement muted-to-ink token harmonization and DS consistency update
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Updated `--muted` in light theme to match ink token value.
2. Updated `--muted` in dark theme to match foreground ink value.
3. Updated design-system token fallback for `--muted` so DS docs reflect the new harmonized value.
2. What was intentionally not done:
1. no component-level rewrites of `text-[color:var(--muted)]`; propagation is token-driven.
2. no backend/API/auth/DB changes.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/index.css`
2. `web/src/lib/design-system.ts`
2. New files:
1. `handoffs/UI-019.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. all static gates green after token change.
3. Manual checks:
1. local screenshot: `qa/reports/screenshots/local-home-muted-ink.png`
2. local screenshot: `qa/reports/screenshots/local-designsystem-muted-ink.png`

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-014`
2. Interfaces changed:
1. frontend design token semantics for `--muted`.
3. Migration or deployment impacts:
1. frontend-only deployment change.

## 6. Open Issues
1. Known issues:
1. none blocking implementation.
2. Risks:
1. placeholder text also inherits stronger contrast due to shared muted token.
3. Follow-up required:
1. QA confirmation and PM signoff.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
