# Handoff: UI-042 Speaker Icon Ink Token Update

## 1. Metadata
1. Task ID: UI-042
2. Title: Apply `ink` color to enabled speaker icon in utility Sound control
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Updated enabled Sound icon (`Volume2`) color class from `brand-700` to `ink`.
2. Kept disabled icon (`VolumeX`) unchanged (already `ink`).
2. What was intentionally not done:
1. no changes to switch behavior or state handling.
2. no layout/spacing changes.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/components/app-shell.tsx`
2. New files:
1. `handoffs/UI-042.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. static gates green.
3. Manual checks:
1. enabled speaker icon now uses `ink` token.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-034`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. none critical.
3. Follow-up required:
1. QA signoff and optional push.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
