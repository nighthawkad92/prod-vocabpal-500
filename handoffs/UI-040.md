# Handoff: UI-040 Progress-Row Star/Label Rendering Update

## 1. Metadata
1. Task ID: UI-040
2. Title: Implement progress-row star render fix and collected-label style alignment
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Replaced CSS-mask based star marker with direct SVG image in progress row to prevent square fallback rendering.
2. Updated copy from `Collect X` to `Collected X`.
3. Switched collected counter to `Badge` styling to match `Question X of 10` typography.
4. Removed vertical separator between question counter and collected counter.
2. What was intentionally not done:
1. no change to question progression logic.
2. no submit CTA behavior changes.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/features/student/student-mode.tsx`
2. New files:
1. `handoffs/UI-040.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. static gates pass.
3. Manual checks:
1. progress row now shows filled star shape, not square.
2. `Collected X` and question badge share badge typography style.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-032`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. none critical.
3. Follow-up required:
1. QA signoff and optional push.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
