# Handoff: UI-041 Q2 Single-Column Layout + Cat-Lottie Mapping

## 1. Metadata
1. Task ID: UI-041
2. Title: Implement Q2 single-column dictation layout and `image-cat.lottie` mapping
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Remapped question 2 visual from `image-dog.lottie` to `image-cat.lottie`.
2. Updated dictation rendering so question 2 uses single-column order: prompt, play audio, full-width visual, answer input.
3. Left all other dictation questions on existing two-column layout.
2. What was intentionally not done:
1. no changes to question 1 layout.
2. no changes to answer submission/audio gate logic.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/features/student/dictation-images.ts`
2. `web/src/features/student/student-mode.tsx`
2. New files:
1. `handoffs/UI-041.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. static gates green.
3. Manual checks:
1. question 2 now renders image below play button in single-column flow.
2. question 2 visual points to bundled `image-cat.lottie`.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-033`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. none critical.
3. Follow-up required:
1. QA signoff and push when requested.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
