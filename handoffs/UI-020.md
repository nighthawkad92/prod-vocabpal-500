# Handoff: UI-020 App Shell Utility-Row Restructure

## 1. Metadata
1. Task ID: UI-020
2. Title: Remove app-shell hero and build utility-row layout
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Removed the legacy hero card from `AppShell` (prototype label + large title + legacy subtitle).
2. Added background-level top utility row with:
1. left conditional logo slot
2. right controls grouped with Sound first, Student/Teacher toggle second
3. Preserved config error alert below utility row and before mode content.
2. What was intentionally not done:
1. no student/teacher form-branding changes in this task.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/components/app-shell.tsx`
2. `web/src/App.tsx`
2. New files:
1. none.
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. all static gates green after shell restructure.
3. Manual checks:
1. utility row control order verified: Sound then mode toggle.

## 5. Downstream Impact
1. Tasks unblocked:
1. `UI-021`
2. Interfaces changed:
1. `AppShellProps` added `showUtilityLogo` and `utilityLogoSrc`.
3. Migration or deployment impacts:
1. frontend-only change.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. responsive alignment needs visual verification on entry/internal states.
3. Follow-up required:
1. entry-card branding integration and QA screenshots.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
