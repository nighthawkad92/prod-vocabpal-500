# Handoff: UI-045 Sentence-Prelude Single-Column + CTA Parity

## 1. Metadata
1. Task ID: UI-045
2. Title: Implement sentence-reading prelude single-column structure and CTA style parity
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Replaced sentence-prelude two-column layout with single-column stack for all prelude screens.
2. Updated image block to full-fill behavior inside a fixed-aspect container.
3. Replaced sentence-only text with instruction line format:
   `QX. Read this sentence: <sentence>`.
4. Moved `Show question` button below sentence and matched its type scale/padding to submit CTA styling.
2. What was intentionally not done:
1. no changes to question-answer screens outside prelude state.
2. no audio/submit state logic changes.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/features/student/student-mode.tsx`
2. New files:
1. `handoffs/UI-045.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. static gates green.
3. Manual checks:
1. prelude screens now render image, instruction text, and CTA in a vertical flow.
2. image fills prelude media container and CTA style matches submit CTA scale/padding.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-037`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. none critical.
3. Follow-up required:
1. QA signoff and optional push.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
