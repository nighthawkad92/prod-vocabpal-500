# Handoff: UI-039 Q1 Single-Column Media Placement + Lottie Remap

## 1. Metadata
1. Task ID: UI-039
2. Title: Implement Q1 single-column media placement + full-width lottie render and asset remap
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Bundled `assets/image-question1.lottie` into web assets.
2. Updated Q1 visual mapping from `image-cat.lottie` to `image-question1.lottie`.
3. Updated MCQ layout logic for Q1 to single-column ordering:
1. Question prompt
2. Play audio button
3. Full-width visual
4. Answer options
4. Added full-width rendering mode for question media visual helper.
2. What was intentionally not done:
1. no change to question 2+ layout behavior.
2. no backend/API/schema changes.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/features/student/dictation-images.ts`
2. `web/src/features/student/student-mode.tsx`
2. New files:
1. `web/src/assets/dictation/image-question1.lottie`
2. `handoffs/UI-039.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. static gates pass.
3. Manual checks:
1. Q1 media appears between audio button and options in single-column flow.
2. Q1 visual fills available width.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-031`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. frontend bundle includes new q1 lottie asset.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. none critical.
3. Follow-up required:
1. QA validation and PM signoff.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
