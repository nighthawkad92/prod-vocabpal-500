# Handoff: UI-050 Collected-Badge Right Align + 4px Gap

## 1. Metadata
1. Task ID: UI-050
2. Title: Right-align collected badge and reduce icon-label gap to 4px
3. Owner Agent: UI
4. Handoff Date: 2026-02-25
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Updated collected badge class from `gap-2` to `gap-1` (4px spacing).
2. Added `ml-auto` to collected badge so it right-aligns within the progress row.
2. What was intentionally not done:
1. no changes to counter copy/icon.
2. no changes to progress bar logic.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/features/student/student-mode.tsx`
2. New files:
1. `handoffs/UI-050.md`
3. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck` -> pass
2. `npm --prefix web run lint` -> pass
3. `npm --prefix web run build` -> pass
2. Test results:
1. static gates green.
3. Manual checks:
1. collected badge aligns to the right of row.
2. star-to-text spacing reduced to 4px.

## 5. Downstream Impact
1. Tasks unblocked:
1. `QA-042`
2. Interfaces changed:
1. none.
3. Migration or deployment impacts:
1. none.

## 6. Open Issues
1. Known issues:
1. none blocking.
2. Risks:
1. none critical.
3. Follow-up required:
1. QA signoff and optional push.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
