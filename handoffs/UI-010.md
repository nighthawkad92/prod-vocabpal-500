# Handoff: UI-010 Route Plumbing + `/designsystem` Shell

## 1. Metadata
1. Task ID: UI-010
2. Title: Route plumbing + `/designsystem` shell
3. Owner Agent: UI
4. Handoff Date: 2026-02-24
5. Proposed Next State: `REVIEW`

## 2. Summary of Work
1. What was completed:
1. Added pathname-based route resolver in app entry with `AppRoute = "app" | "designsystem"`.
2. Split baseline app rendering into `BaselineAssessmentApp` and isolated `/designsystem` rendering path.
3. Added design-system shell page with top-level header, state badges, and back-to-app control.
4. Added Vercel SPA rewrites for `/designsystem` and `/designsystem/*`.
2. What was intentionally not done:
1. no React Router dependency added.

## 3. Files and Artifacts
1. Files changed:
1. `web/src/App.tsx`
2. `vercel.json`
3. New files:
1. `web/src/features/design-system/design-system-page.tsx`
2. Commands executed:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`

## 4. Validation Evidence
1. Tests run:
1. `npm --prefix web run typecheck`
2. `npm --prefix web run lint`
3. `npm --prefix web run build`
2. Test results:
1. all passed.
3. Manual checks:
1. headless browser check confirms `/designsystem` heading renders.

## 5. Downstream Impact
1. Tasks unblocked:
1. `UI-011`
2. Interfaces changed:
1. app now supports direct route rendering for `/designsystem`.
3. Migration or deployment impacts:
1. Vercel rewrite required and included.

## 6. Open Issues
1. Known issues:
1. none.
2. Risks:
1. production route verification required after next deployment.
3. Follow-up required:
1. implement design-system content sections.

## 7. Requested PM Action
1. Mark as `DONE`
2. Return to `RUNNING` with rework scope
3. Move to `BLOCKED` pending checkpoint id
